{
  "name": "misskey-notify-to-discord_tempura",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "lint": "biome check . --write",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.1",
    "@cloudflare/vitest-pool-workers": "^0.8.50",
    "@types/node": "^24.0.12",
    "typescript": "^5.8.3",
    "vitest": "3.2.4",
    "wrangler": "^4.24.0"
  }
}
